Project étape 1

géneration de formulaires de recherche, puis filtres.

Demander au serveur de ravayer une "map" du fichier JSON concerné cad, pour chaque propiété différente, toutes les valeurs différentes:

[
	{"p1": "v11", "p2": "v21", "p3": "v31"},
	{"p1": "v12", "p2": "v21", "p3": "v32"}
]

3D

Scene 
Geometry / Material / Texture
Moteur de rendu (renderer)
Camera
Vue

three.js > WebGl > openGl

var s = THREE.scene();

THREE.CubeGeometry(....);

